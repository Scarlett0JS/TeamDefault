<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.theme.dao.ThemeMapper">

	<select id="allList" resultType="com.theme.entity.Board">
		select * from board order by num
	</select>

	<insert id="boardWrite" parameterType="com.theme.entity.Board">
		Insert into board (num, title, content, writer)
		values (board_num.nextval, #{title}, #{content}, #{writer})
	</insert>

	<delete id="boardDelete" parameterType="int">
		delete from board where num = #{num}
	</delete>

	<select id="boardView" parameterType="int" resultType="com.theme.entity.Board">
		select * from board where num = #{num}
	</select>
	
	<update id="countUpdate" parameterType="int">
		update board set count = count + 1 where num = #{num}
	</update>
	
	<update id="boardUpdate" parameterType="com.theme.entity.Board">
		update board set title = #{title}, content = #{content} where num = #{num}
	</update>
	
	<insert id="MemberSignUp" parameterType="com.theme.entity.Member">
		Insert into MEMBER (memId, memName, memPwd)
		values (#{memId}, #{memName}, #{memPwd})
	</insert>
	
	<select id="MemberLogin" parameterType="com.theme.entity.Member" resultType="com.theme.entity.Member">
		select * from member where memId = #{memId} and memPwd = #{memPwd}
	</select>
	
<<<<<<< HEAD
	<select id="memberList" resultType="com.theme.entity.Member">
		select * from member 
	</select>
	
	<delete id="memberDelete" parameterType="String">
		delete from member where MEMID = #{memId}
	</delete>
=======
	<select id="allColor" resultType="com.theme.entity.Color">
		select * from ColorMap order by col
	</select>
>>>>>>> e7f861c29fdf78e0ad3fdf6e613fd0e0a4939f09
</mapper>